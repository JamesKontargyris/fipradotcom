(function($) {
    //Reveal short bio on profile photo rollover
    //$('.team-member .profile-photo').on('mouseenter', function() {
    //    $(this).next('.profile-details').find('.short-bio').slideDown(300);
    //}).on('mouseleave', function() {
    //    $(this).next('.profile-details').find('.short-bio').slideUp(300);
    //})
})(jQuery);